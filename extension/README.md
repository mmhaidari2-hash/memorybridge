# MemoryBridge companion extension

Load unpacked in Chrome: `chrome://extensions` → Developer mode → Load unpacked → select this `extension/` folder.

Then open `/app/dashboard.html`, click **Link extension**, and use the popup to send a local draft to `/app/records.html`.

The extension stores only the paired app origin. It does not receive workspace API keys and cannot grant paid entitlement.
